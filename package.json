{
  "name": "velocity-tracker",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  },
  "scripts": {
    "test": "node --test",
    "test:browser": "playwright test"
  }
}
